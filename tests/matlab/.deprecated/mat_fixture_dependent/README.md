Deprecated MATLAB Fixture Tests
===============================

This folder contains legacy tests that depend on external `.mat` workspace fixtures.

These tests are not valid blockers for current SRIF/SRIS or shared-module work. Re-enable
them only after replacing the `.mat` dependency with generated or builder-style test data.
