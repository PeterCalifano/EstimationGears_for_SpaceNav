Deprecated MATLAB Tests
=======================

Tests in this folder are excluded from the active validation set.

Current policy: tests that depend on external `.mat` workspace fixtures stay deprecated until
they are reworked to generated or builder-style test data.
